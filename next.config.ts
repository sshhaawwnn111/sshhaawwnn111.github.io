import type { NextConfig } from "next";

const repoName = "My-Portfolio"; // change if your repo name is different

const nextConfig: NextConfig = {
  output: "export",
  /* config options here */
};

export default nextConfig;
